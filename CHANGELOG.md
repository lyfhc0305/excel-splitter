# 更新日志

## 2026-09-25

### 界面

- 改为左侧设置、右侧预览的布局：卡片式分区、统一配色，按系统选用中文字体；Windows 高分屏下不再模糊，并按缩放比例调整窗口和控件尺寸。
- 预览按内容自动调整列宽，自动滚动到关键列，以颜色区分表头、表尾和关键行，用“⋯”标出省略的行；未选择文件、读取中和出错时直接在预览区说明。
- 保留单击列标题设关键列、单击行设关键行；新增右键菜单，可把某行或某列直接设为表头、表尾或左右固定列的分界。
- 关键列下拉框列出全部列（最多 300 列），关键行下拉框列出前 50 行，不再只列出预览中的列。
- 状态栏显示进度条和正在生成的分组，可取消拆分；完成后可直接打开输出目录。
- 增加使用说明、参数悬浮提示和快捷键（Ctrl+O、F5、Ctrl+Enter、Esc）。
- 修复分隔线压住说明文字、预览图例被挤到不可见的问题。

### 新增功能

- 拆分对象可勾选输出，支持全选、全不选、反选；命令行对应 `--group`。
- 自定义文件命名规则，支持 `{文件名}`、`{关键字}`、`{序号}`、`{工作表}`，列表中实时显示每个分组的输出文件名；命令行对应 `--name-template`。
- 可合并为一个工作簿，每个拆分对象一个工作表，公式、名称、打印和筛选按工作表分别调整；命令行对应 `--single-workbook`。
- 命令行 `--list-groups` 只列出拆分对象及其行列数，不生成文件。
- 记住拆分方式、行列参数、输出方式、命名规则和上次打开的文件夹。

### 优化与修复

- 调整参数时不再反复读取同一文件，`.xls` 等需要转换的文件只转换一次；文件在外部保存后自动重新读取。
- 选择其他位置的源文件时，自动生成的输出目录随之更新，手动指定的目录保持不变。
- 日期关键字按 `2026-09-25` 形式分组和命名，`3.0` 与 `3` 等数值按 Excel 显示值归为同组。
- 修复源工作表名为 `sheet`、`SHEET` 等 `Sheet` 的大小写变体时，输出工作表被改名为 `sheet1`，带工作表名的公式因此失效的问题。
- 修复没有页面设置的工作表在每生成一个文件时都整份复制源工作簿的问题：xlsx 内核的 `.et` 和不含 VBA 的 `.xlsm`、`.xltm` 因此报错“cannot pickle '_thread.RLock' object”无法拆分，普通 `.xlsx` 也明显变慢（2 万行拆成 20 个文件由 76 秒降到 5 秒）。
- 只在确实存在空白关键字时提示空白分组。

### 验证

- 51 项自动测试通过，新增 20 项覆盖命名规则、分组选择、合并工作簿、进度与取消、读取缓存、`.et`/`.xlsm` 读取、命令行参数和界面流程。
- 用 xlsx 内核的 `.et`，以及用 `.xls` 内容模拟的二进制 `.et`（经 LibreOffice 转换）验证了读取、预览和拆分，转换只进行一次；尚未用 WPS 生成的真实旧版 `.et` 文件验证。
- 用 LibreOffice 重新计算两种输出方式的拆分结果，逐行公式、表尾合计、工作表级名称及改名后的工作表引用结果均正确。
- 在 Linux（Xvfb）下检查了默认尺寸、最小尺寸和 150% 缩放的界面；尚未在 Windows 实机上检查界面和打包结果。

## 2026-09-05

### 修复

- 防止非法字符、大小写差异、超长关键字或重复运行导致输出文件互相覆盖。
- 按公式语法调整行列引用，避免误改文字和函数名；失效引用在完成提示中明确报告。
- 保留空白关键字分组及拆分后的合并单元格内容。
- 统一两种拆分模式和预览的参数校验，支持零表头与零固定列。
- 调整打印、筛选、冻结和分页范围，补充日期系统、主题、保护、普通条件格式和数据验证的保留。
- 整批暂存输出，保存或发布失败时清理本次文件，避免残留不完整结果。
- 将文件读取、预览与拆分放到后台，增加输入防抖、过期预览丢弃和防重复启动。
- 修复命令行参数错误处理、临时文件释放和转换器超时后的回退。
- 打包前运行测试，失败及时停止，不再清空整个构建及输出目录。

### 支持边界

- 已识别的宏、图表、跨表公式等无法可靠保留的内容会明确提示处理限制；详见 README。
- 公式由 Excel/WPS 打开输出文件时重算，本程序不计算公式结果。

### 验证

- 31 项自动测试通过，包含后台界面响应和错误恢复测试。
- 命令行端到端验证通过：零表头正常拆分，缺少参数和无效参数返回错误状态。
- Python 编译检查与差异格式检查通过。
- 尚未使用真实业务文件验证旧格式转换，也未验证 Linux 打包。

详细问题与修复对应关系见 [审阅记录](REVIEW.md)。
