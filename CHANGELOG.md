# 更新日志

## 2026-09-05

### 修复

- 防止非法字符、大小写差异、超长关键字或重复运行导致输出文件互相覆盖。
- 按公式语法调整行列引用，避免误改文字和函数名；失效引用在完成提示中明确报告。
- 保留空白关键字分组及拆分后的合并单元格内容。
- 统一两种拆分模式和预览的参数校验，支持零表头与零固定列。
- 调整打印、筛选、冻结和分页范围，补充日期系统、主题、保护、普通条件格式和数据验证的保留。
- 整批暂存输出，保存或发布失败时清理本次文件，避免残留不完整结果。
- 将文件读取、预览与拆分放到后台，增加输入防抖、过期预览丢弃和防重复启动。
- 修复命令行参数错误处理、临时文件释放和转换器超时后的回退。
- 打包前运行测试，失败及时停止，不再清空整个构建及输出目录。

### 支持边界

- 已识别的宏、图表、跨表公式等无法可靠保留的内容会明确提示处理限制；详见 README。
- 公式由 Excel/WPS 打开输出文件时重算，本程序不计算公式结果。

### 验证

- 31 项自动测试通过，包含后台界面响应和错误恢复测试。
- 命令行端到端验证通过：零表头正常拆分，缺少参数和无效参数返回错误状态。
- Python 编译检查与差异格式检查通过。
- 尚未使用真实业务文件验证旧格式转换，也未验证 Linux 打包。

详细问题与修复对应关系见 [审阅记录](REVIEW.md)。
