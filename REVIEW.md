# 拆表工具审阅与修复记录

本次审阅对象是源代码。仓库没有实际业务表格，验证使用自动生成的测试工作簿；未使用真实文件验证 Excel/WPS/LibreOffice 的旧格式转换，也未验证 Linux 打包。

验证结果：31 项自动测试全部通过（含实际 Tk 窗口组件的后台响应、过期预览和错误恢复测试）；Python 编译检查与差异格式检查通过。

| 原有缺点 | 影响 | 修复 |
| --- | --- | --- |
| 直接将关键字清洗后作为文件名保存 | `A/B` 与 `A:B`、超长关键字、大小写差异或重复运行可能覆盖文件 | 文件名规范化、忽略大小写查重、自动编号、排他写入 |
| 用正则扫描整个公式 | 误改字符串或函数名；删除引用可能误指新数据 | 按公式词法单元识别引用；单独引用删除单元格时输出 `#REF!` |
| 范围公式只考虑数据区 | 同时包含表头、数据、表尾的范围被截断 | 以完整保留行列集合映射公式范围 |
| 按行拆分要求整个合并区域全部保留 | 部分合并区域及其文字丢失 | 两种模式共用合并收缩与起始单元格迁移逻辑 |
| 空白分组直接跳过 | 没有提示就丢掉记录 | 空白关键字独立成组，并避免与同名实际关键字合并 |
| 将关键字公式文本用来分组 | 相同计算结果可能分成不同文件 | 明确要求先把关键字公式转换为固定值 |
| 预览暗中夹取参数，拆分又使用另一套规则 | 预览数量与实际结果不一致，负数被改成 0 | 共享严格校验，支持零表头，非法参数显示具体错误 |
| 每个文件直接保存到最终路径 | 中途失败留下半批结果或坏文件 | 先保存整批临时文件，再写最终文件；可捕获写入错误时回滚本次文件 |
| 复制静态打印和筛选坐标 | 打印空页、筛选错列、冻结位置和分页不正确 | 随拆分映射范围、筛选列、冻结位置与分页 |
| 遗漏日期系统、主题、保护、条件格式和验证 | 日期或颜色改变、表格行为丢失 | 复制日期系统/主题/保护，映射普通条件格式与验证 |
| 不加载宏却宣称支持宏文件，模板标志与输出扩展名不一致 | 静默丢宏或产生格式不匹配文件 | 检测 VBA 并明确停止；模板正确转换为普通 xlsx |
| 单表输出仍照搬隐藏状态及无效工作簿引用 | 保存失败或跨表公式失效 | 输出表可见，只复制实际使用的可映射名称，不支持的依赖明确报错 |
| 读取与拆分运行在界面线程，每次输入触发重复全量预览 | 大文件导致窗口无响应，旧预览覆盖新参数 | 串行后台任务、输入防抖、过期任务丢弃、预览显示上限、拆分防重复启动 |
| 异常路径未关闭转换文件，转换超时不尝试后续方案 | 文件句柄或临时目录残留 | 统一资源释放；LibreOffice 异常后尝试其他转换器 |
| CLI 缺参数或表头为 0 时误入 GUI | 自动化调用挂起 | 必要参数检查、支持 0、失败返回非零状态 |
| 打包脚本无条件删除整个产物目录，失败仍可能提示成功 | 意外清理无关产物、错误被掩盖 | 去掉广泛删除，先测试并逐步检查失败状态 |

主要实现位于 `src/split_core.py`（共享数据逻辑）与 `src/excel_splitter.py`（读写入口、预览、GUI、CLI）。具体支持边界和运行方式见 README。
